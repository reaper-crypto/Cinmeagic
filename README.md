# MongoDB connection string.
# Local example:    mongodb://127.0.0.1:27017/cinemagic
# Atlas example:    mongodb+srv://<user>:<password>@<cluster>.mongodb.net/cinemagic
MONGODB_URI=mongodb://127.0.0.1:27017/cinemagic

# Port the API server listens on
PORT=5000

# Allowed origin for the frontend (set to your deployed frontend URL in production)
CORS_ORIGIN=*
